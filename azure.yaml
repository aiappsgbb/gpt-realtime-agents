# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: agent-call-center
metadata:
  template: agentic-call-center-with-ai-voice-models@1.0.1
services:
  audio-backend:
    project: .
    language: python
    host: containerapp
    docker:
      context: .
      remoteBuild: true
